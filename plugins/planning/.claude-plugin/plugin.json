{
  "name": "planning",
  "description": "Structured implementation planning, interactive annotation review, and autonomous plan execution. Fork: stages changes via `git add` and never auto-commits — all VCS decisions stay with the user.",
  "version": "3.6.1-no-autocommit",
  "author": {
    "name": "Umputun"
  },
  "homepage": "https://github.com/umputun/cc-thingz",
  "repository": "https://github.com/umputun/cc-thingz",
  "license": "MIT",
  "userConfig": {
    "external_review_cmd": {
      "title": "External review command",
      "type": "string",
      "description": "Command for external code review tool. Leave empty to auto-detect codex.",
      "default": ""
    },
    "task_retries": {
      "title": "Task retries",
      "type": "number",
      "description": "Number of retries for failed tasks before stopping.",
      "default": 1
    },
    "review_iterations": {
      "title": "Review iterations",
      "type": "number",
      "description": "Max fix-and-recheck cycles during internal review.",
      "default": 5
    },
    "external_review_iterations": {
      "title": "External review iterations",
      "type": "number",
      "description": "Max iterations for external review adversarial loop.",
      "default": 10
    },
    "finalize_enabled": {
      "title": "Enable finalize",
      "type": "boolean",
      "description": "Always skipped in this fork (no commits exist to rebase or squash). Kept for compatibility; the value is ignored.",
      "default": false
    },
    "plans_dir": {
      "title": "Plans directory",
      "type": "string",
      "description": "Directory where plan files are located, relative to project root.",
      "default": "docs/plans"
    }
  }
}
